{
  "required": true,
  "$schema": "http://json-schema.org/draft-03/schema",
  "type": "object",
  "title": "Open Date Ticket Redemption",
  "properties": {
    "id": {
      "required": true,
      "type": "string",
      "title": "Open Date Ticket ID",
      "description": "unique identifier of the open date ticket that was redeemed"
    },
    "order_id": {
      "required": true,
      "type": "string",
      "title": "Order ID",
      "description": "The unique identifier of the order that was created during redemption process"
    },
    "appointment_id": {
      "required": true,
      "type": "string",
      "title": "Appointment ID",
      "description": "The unique identifier of the appointment the open date ticket was booked into"
    }
  }
}